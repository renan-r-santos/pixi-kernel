[project]
name = "pixi_project"
version = "0.1.0"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "win-64"]

[dependencies]
python = "*"
ipykernel = "*"

[feature.test.dependencies]

[environments]
test = ["test"]
